{% do craft.eagerBeaver.eagerLoadElements(entry, [
    'hero:target',
    ])
%}

{%- include '_organisms/heroes/hero--primary' ignore missing with {
    heading: entry.hero.collect().one().heading ??? entry.title ??? '',
    description: entry.hero.collect().one().description ??? '',
    button: entry.hero.collect().one().target ??? null
} -%}

{%- include '_organisms/builders/builder--content' with {
    content: entry,
} -%}