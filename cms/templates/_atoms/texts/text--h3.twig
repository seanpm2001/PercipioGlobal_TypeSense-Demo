{%- extends '_atoms/texts/_text--props' -%}

{#
    ### PROPERTIES ###
    ------------------

    INHERITED FROM: `_atoms/texts/text--props.twig`

    OMITTED
    ---------

    alignment
#}

{%- block text -%}

    {%- set classes = {
        '_default': '',
        'font': 'font-primary font-medium text-base md:text-lg',
    }-%}

    {%- minify -%}
        <span class="
            {{- classes._default }} 
            {{ classes.font }}
            {{ props.utilities -}}
        ">
            {{- props.content | typogrify(true) | raw -}}
        </span>
    {%- endminify -%}

{%- endblock -%}
