{#
    ### PROPERTIES ###
    ------------------

    icon: (Asset) - the SVG asset
    utlities: (STR) - the icon utilities
#}

{%- set props = {
    icon: options.icon ??? null,
    utilities: options.utilities ??? null,
} -%}

{%- if props.icon -%}
    <div class="{{- props.utilities -}}">
        {{- svg(props.icon, sanitize=false, namespace=false) |attr({ class: 'w-full' }) -}}
        {# {%- include 'svgs/' ~ props.icon ~ '.svg' -%} #}
    </div>
{%- endif -%}
